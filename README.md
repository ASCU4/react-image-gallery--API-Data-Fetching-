📸 React Image Gallery – API Data Fetching

This is a simple **React application** that fetches image data from a public API and displays it dynamically on the user interface.  
The main goal of this project is to understand **API integration**, **state management**, and **dynamic rendering** in React.

---

## 🚀 Features

- Fetches image data from a public API using **Axios**
- Displays images dynamically using **React state**
- Shows image author names
- Uses the **map()** function to render multiple items
- Simple and clean UI

---

## 🛠️ Tech Stack

- **React**
- **JavaScript (ES6)**
- **Axios**
- **Vite**
- **Tailwind CSS**
